name: Сборка
on:
  push:
jobs:
  build-test:
    name: Test context
    runs-on: ubuntu-cloud-runner
    container:
      gradle:ubi-minimal
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: |
          ./gradlew build