name: Сборка
on:
  push:
jobs:
  build-test:
    name: Test context
    runs-on: ubuntu-cloud-runner
    container:
      gradle:jdk17-alpine
    steps:
      - name: Checkou
        uses: actions/checkout@v3
      - name: Build
        run: |
          ./gradlew build